import Message from './Message';
export default class Relay {
    readonly url: string;
    encoding: 'json' | 'binary';
    constructor(url: string, encoding?: 'json' | 'binary');
    private fetch;
    send(message: Message): Promise<void>;
    private jsonRequest;
    private binaryRequest;
}
