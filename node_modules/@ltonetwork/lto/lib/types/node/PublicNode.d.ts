import Transaction from '../transactions/Transaction';
export default class PublicNode {
    readonly url: string;
    readonly apiKey: string;
    constructor(url: string, apiKey?: string);
    private fetch;
    post(endpoint: string, postData: any, headers?: Record<string, string>): Promise<any>;
    get(endpoint: string, headers?: Record<string, string>): Promise<any>;
    broadcast<T extends Transaction>(transaction: T): Promise<T>;
    submit<T extends Transaction>(transaction: T): Promise<T>;
    status(): Promise<{
        blockchainHeight: number;
        stateHeight: number;
        updatedTimestamp: number;
        updatedDate: string;
    }>;
    version(): Promise<string>;
}
